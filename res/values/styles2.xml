<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:buttonStyle">@style/PiViButton</item>
        <item name="android:windowBackground">@drawable/background</item> 
        <item name="android:colorBackground">@color/transparent</item>        
        <item name="android:textColor">@color/text_color</item>
        <item name="android:spinnerStyle">@style/PiViSpinner</item>
        <item name="android:editTextStyle">@style/PiViEditText</item>
        
        <item name="actionBarStyle">@style/Widget.MyTheme.ActionBar</item>
    </style>
    
    <!-- Sherlock Theme -->
    <style name="Theme.Sherlock.Pivi" parent="Theme.Sherlock">
        <item name="actionBarStyle">@style/Widget.MyTheme.ActionBar</item>
    </style>
    
	<style name="Widget.MyTheme.ActionBar" parent="Widget.Sherlock.ActionBar">
	    <item name="android:background">#ffff0000</item>
	    <item name="background">#ffff0000</item>
	</style>
    <!-- Sherlock Theme -->
    
    <style name="PiViButton" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/button</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="android:minHeight">48dip</item>
        <item name="android:minWidth">64dip</item>
        <item name="android:textAppearance">@android:style/TextAppearance.Medium</item>
    </style>
    
    <style name="PiViSpinner" parent="@android:style/Widget.Spinner">
        <item name="android:background">@drawable/spinner_default_pivi</item>
        <item name="android:popupBackground">@drawable/spinner_popup</item>
        <item name="android:textColor">@color/text_color</item>
    </style>
    
    <style name="PiViEditText" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/textfield_default_pivi</item>
        <item name="android:textColor">@color/text_color</item>
    </style>
    
    <style name="PiViTabView" parent="@style/Widget.Sherlock.ActionBar.TabView">
    	<!-- <item name="android:background">@drawable/tab_selected_pivi</item> -->
    </style>

</resources>